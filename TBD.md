# 개발 및 협업 규칙

이 문서는 Reminiscence-BE의 개발 흐름과 협업 규칙을 정의한다.
프로젝트가 진행되며 팀 합의가 바뀌면 이 문서를 함께 갱신한다.

## 브랜치 전략

### 장기 브랜치

- `main`: 배포 가능한 운영 브랜치
- `dev`: 다음 배포를 준비하는 통합 브랜치

`main`과 `dev`에는 직접 커밋하지 않는다. 모든 변경은 작업 브랜치에서
Pull Request(PR)를 통해 병합한다.

### 작업 브랜치

브랜치 이름은 `<type>/#<issue-number>-<title>` 형식을 사용한다.
`title`은 작업 내용을 알 수 있는 짧은 영문 kebab-case로 작성한다.

| 유형 | 용도 | 분기 기준 | 예시 |
| --- | --- | --- | --- |
| `feat` | 신규 기능 | `dev` | `feat/#12-create-memory-api` |
| `fix` | 일반 버그 수정 | `dev` | `fix/#24-handle-empty-title` |
| `hotfix` | 운영 긴급 수정 | `main` | `hotfix/#31-fix-login-failure` |
| `refactor` | 동작 변경 없는 구조 개선 | `dev` | `refactor/#42-split-user-service` |
| `docs` | 문서 변경 | `dev` | `docs/#18-update-api-guide` |
| `test` | 테스트 추가·수정 | `dev` | `test/#27-add-auth-edge-cases` |
| `chore` | 빌드, 설정, 의존성 등 | `dev` | `chore/#35-update-dependencies` |

- 작업을 시작하기 전에 이슈를 만든다.
- 하나의 브랜치는 하나의 이슈와 하나의 목적만 다룬다.
- 서로 독립적으로 리뷰하거나 되돌릴 수 있는 변경은 브랜치와 PR도 분리한다.
- 작업 브랜치는 병합 후 삭제한다.

## Pull Request 규칙

### 대상 브랜치

- 일반 작업: 작업 브랜치 → `dev`
- 정기 배포: `dev` → `main`
- 긴급 수정: `hotfix/#<issue-number>-<title>` → `main`
- 핫픽스 배포 후: `main` → `dev` 동기화 PR을 즉시 생성

### 제목과 본문

PR 제목은 `[<Type>] #<issue-number> <summary>` 형식을 사용한다.

예시:

```text
[Feat] #12 추억 생성 API 추가
```

PR 본문에는 다음 내용을 포함한다.

```markdown
## 관련 이슈
- Closes #12

## 변경 내용
-

## 검증 방법
-

## 참고 사항
-

## 체크리스트
- [ ] 변경 범위가 하나의 목적에 집중되어 있다.
- [ ] 필요한 테스트를 추가하거나 갱신했다.
- [ ] 테스트, 린트, 타입 검사가 통과한다.
- [ ] API 또는 설정 변경을 문서에 반영했다.
- [ ] 비밀값과 개인정보가 포함되지 않았다.
```

### 리뷰와 병합

- 리뷰 가능한 크기로 PR을 유지한다. 목적이 둘 이상이면 PR을 분리한다.
- 리뷰 준비 전에는 Draft PR을 사용한다.
- 최소 1명의 승인을 받고 필수 CI가 모두 통과한 뒤 병합한다.
- 리뷰 의견은 반영하거나, 반영하지 않는 이유를 답변한 뒤 resolve한다.
- 일반 작업 브랜치는 `dev`에 squash merge한다.
- `dev`에서 `main`으로 가는 배포 PR은 변경 이력을 보존하도록 merge commit을
  사용한다.
- force push와 rebase는 개인 작업 브랜치에서만 허용한다.

## 이슈 규칙

이슈 제목은 `[<Type>] <summary>` 형식을 사용한다.

```text
[Feat] 추억 생성 API 구현
[Bug] 제목이 비어 있을 때 500 응답 발생
```

이슈 본문에는 다음 내용을 포함한다.

- 배경과 목적
- 작업 범위
- 완료 조건(acceptance criteria)
- 제외 범위
- 관련 자료 또는 선행 이슈

버그 이슈에는 재현 절차, 기대 결과, 실제 결과, 실행 환경을 추가한다. 로그와
스크린샷을 첨부할 때는 토큰, 비밀번호, 개인정보를 제거한다.

- `type`, `status`, `priority` 라벨을 지정한다.
- 담당자가 정해지면 assignee를 지정한다.
- 배포 일정이 있는 작업은 milestone에 연결한다.
- 중복 이슈는 원본 이슈를 링크한 뒤 닫는다.

## 커밋 규칙

커밋 메시지는 `<type>: <summary> (#<issue-number>)` 형식을 사용한다.

```text
feat: add memory creation endpoint (#12)
fix: reject an empty memory title (#24)
```

- 제목은 명령형 영문으로 간결하게 작성한다.
- 커밋 하나에는 하나의 목적만 담는다.
- 구현과 그 구현을 검증하는 테스트는 같은 커밋에 포함한다.
- 설정·인프라 변경과 도메인 구현은 별도 커밋으로 분리한다.
- 빌드나 테스트가 깨지는 중간 상태는 공유 브랜치에 푸시하지 않는다.

## API 변경 규칙

- API 계약은 FastAPI가 생성하는 OpenAPI 문서와 일치해야 한다.
- 요청·응답 모델에는 타입, 필수 여부, 유효성 조건을 명시한다.
- 하위 호환되지 않는 변경은 이슈와 PR에 영향 범위와 전환 방법을 기록한다.
- 오류 응답은 일관된 형식을 사용하고 예상 가능한 오류를 테스트한다.
- 새 환경 변수는 예제 환경 파일과 실행 문서에 함께 추가하며 실제 비밀값은
  커밋하지 않는다.

## 완료 기준

변경 성격상 해당 항목이 존재할 때 다음 조건을 모두 만족해야 한다.

- 기능과 주요 엣지 케이스를 검증하는 테스트가 통과한다.
- linter가 오류 없이 통과한다.
- 타입 검사가 오류 없이 통과한다.
- 관련 API·설정·운영 문서가 최신 상태다.
- PR의 완료 조건과 체크리스트가 충족되었다.

## 릴리스 규칙

- `main`의 모든 커밋은 배포 가능한 상태여야 한다.
- 버전은 Semantic Versioning(`MAJOR.MINOR.PATCH`)을 따른다.
- 릴리스 태그는 `v<MAJOR>.<MINOR>.<PATCH>` 형식을 사용한다.
- 배포 PR에는 포함된 변경, 마이그레이션 여부, 롤백 방법을 기록한다.
