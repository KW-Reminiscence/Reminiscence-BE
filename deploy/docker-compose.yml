services:
  api:
    image: ${IMAGE_NAME:?IMAGE_NAME is required}:${IMAGE_TAG:?IMAGE_TAG is required}
    container_name: ${CONTAINER_NAME:?CONTAINER_NAME is required}
    restart: unless-stopped
    network_mode: host
    command:
      - uvicorn
      - reminiscence.main:app
      - --host
      - 127.0.0.1
      - --port
      - "${HOST_PORT:?HOST_PORT is required}"
      - --proxy-headers
    security_opt:
      - no-new-privileges:true
    read_only: true
    env_file:
      - ${RUNTIME_ENV_FILE:?RUNTIME_ENV_FILE is required}
    environment:
      NOTIFICATION_CONFIG_PATH: /run/secrets/notification-config.json
      REMINISCENCE_DATA_DIR: /data
      SUPERTONIC_MODEL_DIR: /models/supertonic-3
    tmpfs:
      - /tmp
    volumes:
      - type: bind
        source: ${DATA_DIRECTORY:?DATA_DIRECTORY is required}
        target: /data
        bind:
          create_host_path: false
      - type: bind
        source: ${NOTIFICATION_CONFIG_FILE:?NOTIFICATION_CONFIG_FILE is required}
        target: /run/secrets/notification-config.json
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SUPERTONIC_MODEL_DIRECTORY:?SUPERTONIC_MODEL_DIRECTORY is required}
        target: /models/supertonic-3
        bind:
          create_host_path: false
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          from urllib.request import urlopen; assert
          urlopen('http://127.0.0.1:${HOST_PORT}/health', timeout=2).status == 200
      interval: 15s
      timeout: 3s
      start_period: 10s
      retries: 3
    stop_grace_period: 20s
